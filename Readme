Known issues:
- MainTripsCollectionViewDataProvider не покрыт unit-тестами от слова совсем. Причина: сильная зависимость от parentVC, 
из-за большого количества разных collectionView

Fixed:
- Если в поля поиска ввести неверные значения адреса (что-то вроде fjnfrfi), то приложение с успехом ляжет 
с пробросом ошибки декодирования в try JSONDecoder().decode... (Добавилена обработка statusCode)

- Если на устройстве будет отсутстовать интернет, приложение ляжет, пробросив ошибку json serialization (Добавлен connectionManager)

- Если свайпнуть от края экрана назад на стартовом экране, интерфейс сломается. Необходим дебаг (Вырезана возможность свайпать для навигации)

